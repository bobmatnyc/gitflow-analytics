#!/bin/bash
# Development runner for gitflow-analytics
# This script runs gitflow-analytics directly from the source code
# without requiring installation

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export PYTHONPATH="${SCRIPT_DIR}/src:$PYTHONPATH"

python -m gitflow_analytics.cli "$@"